var HandleDataXocDia = cc.Class({
    extends: cc.Component,
    statics: {
        getIns() {
          if (this.self == null) this.self = new HandleDataXocDia();
          return this.self;
        },
      },
    
    properties: {
       
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    start () {

    },

    reviceData(responseCode, packet){
        switch (key) {
          case value:
            
            break;
        }
    },

    // update (dt) {},
});
module.exports = HandleDataXocDia;